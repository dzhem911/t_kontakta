version: '3'

services:
  web:
    build: ./t_kontakta #dzhem911/tires_project_web_1 #
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - ./t_kontakta:/data
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: dzhem911/tires_project_db_1 #postgres
    restart: always
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=postgres
      - PGPASSWORD=password

